<h1>Session Log in</h1>

<%= form_for(@user, url: new_sessions_path) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>
  <label> Password </label><br>
  <%= f.password_field :password_digest %>
  <%= f.submit "Log in" %>
<% end %>